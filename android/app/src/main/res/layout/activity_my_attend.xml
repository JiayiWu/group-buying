<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools" xmlns:toolbar="http://schemas.android.com/apk/res-auto"
        android:id="@+id/activity_my_attend"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/greyText"
        tools:context="com.mergeorder.presentation.activity.me.MyAttendActivity">

    <android.support.v7.widget.Toolbar
            android:id="@+id/my_attend_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:layout_alignParentTop="true"
            android:background="@color/colorPrimary"
            toolbar:title="@string/me_label_merge"
            toolbar:titleTextColor="@color/white"
            toolbar:navigationIcon="@drawable/ic_close_24dp"/>

    <com.mergeorder.presentation.view.TopicListView
            android:id="@+id/my_attend_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_below="@id/my_attend_toolbar"
            android:divider="@color/greyText"
            android:dividerHeight="8dp">

    </com.mergeorder.presentation.view.TopicListView>

</RelativeLayout>
